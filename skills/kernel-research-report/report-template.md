# {报告标题}

> **调研主题**: {主题描述}
> **内核版本**: {版本/分支信息}
> **生成日期**: {YYYY-MM-DD}

---

## 1. 概述

{简要介绍调研主题的背景、在内核中的定位和作用。1-3段。}

### 1.1 关键术语

| 术语 | 全称 | 说明 |
|------|------|------|
| {缩写} | {英文全称} | {中文简要说明} |

## 2. 架构设计

{描述整体架构和组件关系。}

### 2.1 架构图

```
{ASCII 架构图}
```

### 2.2 核心组件

{逐一介绍关键组件的职责和接口。}

## 3. 源码分析

### 3.1 关键数据结构

{列出并解释核心数据结构，附源码引用。}

```c
// [文件名](文件路径:行号)
{关键结构体或代码片段}
```

### 3.2 初始化流程

{描述初始化调用链。}

```
函数A()
  -> 函数B()
    -> 函数C()
```

### 3.3 运行时流程

{描述运行时的关键路径，如中断处理、调度决策等。}

## 4. 规范对照

{将内核实现与硬件/软件规范进行对照分析。}

| 规范要求 | 规范章节 | 内核实现 | 备注 |
|----------|----------|----------|------|
| {要求描述} | [{规范名}] Section X.Y | {实现位置和方式} | {差异或说明} |

## 5. 方案对比

{如果存在多种实现方式或设计选择，进行对比分析。}

### 5.1 对比总览

| 维度 | 方案 A: {名称} | 方案 B: {名称} |
|------|---------------|---------------|
| 原理 | {简述} | {简述} |
| 性能 | {评估} | {评估} |
| 复杂度 | {评估} | {评估} |
| 适用场景 | {描述} | {描述} |
| 局限性 | {描述} | {描述} |

### 5.2 方案 A: {名称}

{详细描述该方案的设计原理、实现细节和关键代码路径。}

**核心思路**：{一句话概括}

**关键实现**：

```c
// [文件名](文件路径:行号)
{关键代码片段}
```

**调用链路**：

```
{入口函数}()
  -> {核心函数}()
    -> {底层函数}()
```

**优势与局限**：
- 优势：{具体描述}
- 局限：{具体描述}

### 5.3 方案 B: {名称}

{同上结构，详细描述该方案。}

### 5.N 小结

{总结各方案的核心差异和选型建议。}

## 6. 跨架构对比

{对比同一技术点在不同架构下的实现方案。RISC-V、ARM64、x86 为必选对比架构，
其他架构（如 MIPS、LoongArch、PowerPC 等）如存在对应实现，也需加入对比。}

### 6.1 对比总览

{注：表格列数根据实际参与对比的架构动态扩展。}

#### 6.1.1 硬件机制对比

| 对比维度 | RISC-V | ARM64 | x86 | {其他架构}... |
|----------|--------|-------|-----|--------------|
| 硬件控制器/机制 | {名称及简述} | {名称及简述} | {名称及简述} | {名称及简述} |
| 规范标准 | {对应规范} | {对应规范} | {对应规范} | {对应规范} |
| 寄存器接口 | {关键寄存器} | {关键寄存器} | {关键寄存器} | {关键寄存器} |

#### 6.1.2 内核实现对比

| 对比维度 | RISC-V | ARM64 | x86 | {其他架构}... |
|----------|--------|-------|-----|--------------|
| 核心源文件 | `arch/riscv/...` | `arch/arm64/...` | `arch/x86/...` | `arch/xxx/...` |
| 关键数据结构 | {结构体名} | {结构体名} | {结构体名} | {结构体名} |
| 初始化入口 | {函数名} | {函数名} | {函数名} | {函数名} |
| 运行时路径 | {关键调用链} | {关键调用链} | {关键调用链} | {关键调用链} |
| 软件抽象层 | {描述} | {描述} | {描述} | {描述} |

### 6.2 RISC-V 实现详解

{详细描述 RISC-V 架构下该技术点的实现。}

**硬件基础**：{硬件控制器/机制及其特点}

**关键实现**：

```c
// [文件名](arch/riscv/.../xxx.c:行号)
{关键代码片段}
```

**调用链路**：

```
{入口函数}()
  -> {核心函数}()
    -> {底层函数}()
```

**设计特点**：
- {特点 1}
- {特点 2}

### 6.3 ARM64 实现详解

{详细描述 ARM64 架构下该技术点的实现。}

**硬件基础**：{硬件控制器/机制及其特点，如 GIC、SMMU 等}

**关键实现**：

```c
// [文件名](arch/arm64/.../xxx.c:行号)
{关键代码片段}
```

**调用链路**：

```
{入口函数}()
  -> {核心函数}()
    -> {底层函数}()
```

**设计特点**：
- {特点 1}
- {特点 2}

### 6.4 x86 实现详解

{详细描述 x86 架构下该技术点的实现。}

**硬件基础**：{硬件控制器/机制及其特点，如 APIC、IOAPIC 等}

**关键实现**：

```c
// [文件名](arch/x86/.../xxx.c:行号)
{关键代码片段}
```

**调用链路**：

```
{入口函数}()
  -> {核心函数}()
    -> {底层函数}()
```

**设计特点**：
- {特点 1}
- {特点 2}

### 6.X {其他架构} 实现详解

{如果调研中发现其他架构（如 MIPS、LoongArch、PowerPC 等）也有对应实现，
按同样结构为每个架构添加独立子章节。此节可重复，按需增减。}

**硬件基础**：{硬件控制器/机制及其特点}

**关键实现**：

```c
// [文件名](arch/xxx/.../xxx.c:行号)
{关键代码片段}
```

**调用链路**：

```
{入口函数}()
  -> {核心函数}()
    -> {底层函数}()
```

**设计特点**：
- {特点 1}
- {特点 2}

### 6.N-1 设计差异分析

{深入分析各架构设计思路的差异，包括：
- 为什么会有这些差异（硬件约束、历史演进、设计哲学等）
- 各自的优势和不足
- 可相互借鉴之处}

### 6.6 小结

{简要总结跨架构对比的关键发现。}

## 7. 配置选项

{列出相关的 Kconfig 选项及其影响。}

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `CONFIG_XXX` | {y/n/m} | {功能说明} |

## 8. 使用方法

{描述如何在实际开发或系统中使用该技术特性，包括启用方式、编程接口、用户态工具等。}

### 8.1 启用与配置

{说明如何在内核中启用该特性，包括必要的 Kconfig 选项、设备树 (DTS) 配置、
内核启动参数 (cmdline) 等。}

**Kconfig 配置示例**：

```
CONFIG_XXX=y
CONFIG_YYY=m
```

**设备树配置示例**（如适用）：

```dts
// {设备树节点示例}
{node-name} {
    compatible = "xxx,yyy";
    {关键属性} = <{值}>;
};
```

**内核启动参数**（如适用）：

```
{参数名}={值}    # {说明}
```

### 8.2 内核态接口

{描述内核模块或驱动开发者可用的 API 和接口。}

| 函数/宏 | 头文件 | 说明 |
|---------|--------|------|
| `{函数名}()` | `{include/路径}` | {功能简述} |

**使用示例**：

```c
// {简要说明使用场景}
#include <{头文件}>

{示例代码片段，5-15 行}
```

### 8.3 用户态接口

{描述用户态程序如何与该特性交互，包括 sysfs/procfs 节点、ioctl 接口、
系统调用等。如不适用可注明"该特性无直接用户态接口"。}

**sysfs/procfs 节点**（如适用）：

| 路径 | 权限 | 说明 |
|------|------|------|
| `/sys/...` 或 `/proc/...` | {读/写} | {功能说明} |

**用户态工具**（如适用）：

```bash
# {工具名称和用途}
{命令示例}
```

### 8.4 调试与诊断

{描述在开发和调试中常用的方法和工具。}

- **内核日志**：关键的 `pr_info`/`pr_debug` 输出和含义
- **ftrace/tracepoint**：可用的跟踪点
- **debugfs 节点**（如适用）：调试文件系统中暴露的信息
- **常见问题排查**：典型故障场景及排查思路

## 9. 总结

{总结关键发现、设计亮点、潜在改进点。}

## 参考资料

1. {内核文档或源码文件引用}
2. {规范文档引用，含版本号}
3. {其他参考资料}
